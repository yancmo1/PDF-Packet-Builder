import Foundation

// Intentionally empty.
//
// The `MailDraft` model lives in `SendLog.swift` so it is guaranteed to be part of the
// app target build without requiring Xcode target membership changes.
